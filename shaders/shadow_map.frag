// shadow_map.frag

#version 450



// precision highp float;
//layout (location = 0) in vec4 inPos;


void main()
{
//   float near = 1.0;
//   float far = 25.0;
//   float z = gl_FragCoord.z;

//   float outval = (2.0 * near) / (far + near - z * (far - near));

// //gl_FragDepth = inPos.z;

// //  color = vec4(vec3(gl_FragCoord.w), 1.0);
//   //color = vec4(vec3(inPosition.z), 1.0);
//   gl_FragDepth = z;//outval;

//   if (gl_FragCoord.z == 0.0)
//   {
//     gl_FragDepth = 1.0;
//   }
  // else
  // {
  //   //gl_FragDepth = gl_FragCoord.w;
  //   gl_FragDepth = 0.0;
  // }
  //color = 0.3;
}
